@model MvcPL.Models.ProfileViewModel
@{
    ViewBag.Title = "UserDetails";
    if (ViewBag.Layout == true) { Layout = null; }
}
@Html.Partial("_UserProfile", Model)

<hr />

@Html.Action("UserFileView", new { id = ViewBag.Id, layout = true })

